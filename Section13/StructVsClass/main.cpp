/* Struct 
comes from the C programming language
declare struct like a class 
the same as class however members are PUBLIC by default unlike PRIVATE by default in normal classes.

 
use struct:
 * for passive objecccts with public access
 * no methods are declared in the struct
use class:
 * for active objects with private access
 * implement getter and setters
 * implement member methods 
*/ 